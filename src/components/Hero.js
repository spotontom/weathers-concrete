import React from "react";
import { StyledHero } from "./Hero.styles";

export default function Hero() {
  return (
    <StyledHero>
      <h2>Quality Concrete Work</h2>
      <p>
        Serving Bay County and the Gulf Coast with exceptional concrete services — patios,
        driveways, slabs, and more.
      </p>
    </StyledHero>
  );
}